{
  "name": "hello-solidity",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "deploy": "npx hardhat run scripts/deploy.js --network rinkeby",
    "interact": "npx hardhat run scripts/interact.js --network rinkeby",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "victorkamoto",
  "license": "ISC",
  "devDependencies": {
    "@nomiclabs/hardhat-ethers": "^2.0.5",
    "hardhat": "^2.9.3"
  },
  "dependencies": {
    "dotenv": "^16.0.0"
  }
}
